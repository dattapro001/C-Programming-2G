#include <stdio.h>
#include <conio.h>
void main()
{
float Length,answer,cm,feet;
printf("Enter the value of length:");
scanf("%f",&Length);
Length=Length*30;
Length=(Length/3.26)*100;
printf("The answer of length in cm is:%f",Length);
}
